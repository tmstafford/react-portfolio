import React from 'react'

function About() {
    return (
        <div className="section">
            <h2 className="title">About Me</h2>
            <div className="s-flex">
                <div className="about-img">Image</div>
                <div className="about-description">
                    We can also create an individual style file for each component and import the style files in the component, we can divide the styling into multiple files and import it as per the requirement in the component. 
                    CSS Files are imported in the same way as we import script files. We can also create an individual style file for each component and import the style files in the component, we can divide the styling into multiple 
                    files and import it as per the requirement in the component. CSS Files are imported in the same way as we import script files
                </div>
            </div>
        </div>
    )
}

export default About;
